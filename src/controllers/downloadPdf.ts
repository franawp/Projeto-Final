/*import BancoDados from "../database/funcitonsDataBase";

export default function exportarArquivo (identificadorLivro: number) {
    const arquivoPDF = BancoDados.recuperarArquivoPDFNoBancoDeDados(identificadorLivro);

    return arquivoPDF;
}
*/

